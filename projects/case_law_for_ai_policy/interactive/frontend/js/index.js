'use strict';

(function () {
  function Typist(dom) {
    this._dom = dom;
  }

  Typist.prototype.type = function (text) {

  }

  function Chatter(dom) {
    this._dom = dom;
  }

  Chatter.prototype.sendSys = function () {
    var dom = _();
    return dom;
  }

  Chatter.prototype.sendMsg = function (mode) {
    var dom = _();
    return dom;
  }

  window.addEventListener('load', function () {

  });
})();
