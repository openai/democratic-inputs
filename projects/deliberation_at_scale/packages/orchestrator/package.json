{
  "name": "@deliberation-at-scale/orchestrator",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && cp -n .env.example .env",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "swc src -d dist",
    "db:generate-types": "dotenv -e .env -- sh -c 'supabase gen types typescript --project-id \"$SUPABASE_PROJECT_ID\" --schema public > ./src/generated/database.types.ts'",
    "db:login": "supabase login",
    "start": "npm run build && node dist/runner.js",
    "lint": "eslint src --ext .ts",
    "clean": "rimraf ./node_modules"
  },
  "keywords": [],
  "author": "Lei Nelissen <lei@bmd.studio>",
  "license": "MIT",
  "dependencies": {
    "@supabase/supabase-js": "^2.32.0",
    "dotenv": "^16.3.1",
    "graphile-worker": "^0.13.0",
    "openai": "^4.0.0-beta.8"
  },
  "devDependencies": {
    "@swc-node/register": "^1.6.6",
    "@swc/cli": "0.1.62",
    "@swc/core": "1.3.84",
    "@types/node": "^20.4.9",
    "rimraf": "5.0.1"
  }
}
