import { Helpers } from "graphile-worker";

import { BaseProgressionWorkerTaskPayload } from "src/types";

export default function enrichtIntroductionParticipants(payload: BaseProgressionWorkerTaskPayload, helpers: Helpers) {
}
