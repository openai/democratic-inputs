
mutation PingParticipant($participantId: UUID!, $lastSeenAt: Datetime!) {
    updateparticipantsCollection(filter: {id: {eq: $participantId} }, set: {last_seen_at: $lastSeenAt}) {
        affectedCount
    }
}
